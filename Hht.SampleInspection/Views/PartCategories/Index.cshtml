@model IEnumerable<Hht.SampleInspection.Models.PartCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PartCategoryDesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PartCategoryDesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PartCategoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PartCategoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PartCategoryId })
        </td>
    </tr>
}

</table>
